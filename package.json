{
  "name": "collaborative-workspace",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@10.9.7",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node -e \"const svc = process.env.RAILWAY_SERVICE_NAME || ''; const workspace = svc.includes('web') ? 'web' : 'api'; console.log('Starting workspace:', workspace); require('child_process').spawn('npm', ['run', 'start', '--workspace=' + workspace], { stdio: 'inherit', shell: true });\""
  },
  "devDependencies": {
    "turbo": "^2.9.7"
  }
}
